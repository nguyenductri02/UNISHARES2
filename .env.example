# API Configuration
REACT_APP_API_URL=http://localhost:8000

# Pusher configuration for real-time features
# Get these values from your Pusher dashboard at https://pusher.com/
REACT_APP_PUSHER_KEY=your_pusher_app_key_here
REACT_APP_PUSHER_CLUSTER=your_pusher_cluster_here
REACT_APP_PUSHER_SECRET=your_pusher_secret_here

# Note: If you don't have Pusher setup, the app will fall back to polling
# Real-time features will work but with a delay
